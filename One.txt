I have built a reusable AutoML framework that trains and tunes multiple models automatically.
Before training, I want to add a mandatory Feature Leakage Detection step.

Please do the following:

1. Explain briefly what feature leakage is and why it is dangerous in AutoML.
2. Design a fast, lightweight leakage detection module that runs BEFORE model training.
3. The module should:
   - Detect features that are highly correlated with the target
   - Detect target-derived or post-outcome features
   - Flag suspicious features using correlation, mutual information, or name-based heuristics
4. The detection must work for:
   - Numeric features
   - Categorical features
   - Text features (basic checks)
5. The outcome should be:
   - A list of suspected leakage features
   - A warning or hard-block option before training starts
6. The solution must:
   - Be model-agnostic
   - Add minimal overhead to training time
   - Integrate cleanly into an AutoML pipeline

Do NOT use heavy models for detection.
Prefer fast statistical checks and heuristics.
Provide a clean design that can scale to many datasets.
