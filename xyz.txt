I need you to UPDATE THE SYSTEM by fixing the issues described below.
Please implement the changes directly and explain briefly what you changed.

=====================================
PROBLEM 1 — UI TEXT LOOKS TOO GREY
=====================================

Across my entire Streamlit UI, most text appears grey and low-contrast:
- Section descriptions
- Helper/instructional text
- Sidebar navigation labels
- Tab labels
- Status indicators

This makes the UI look dim and slightly hard to read, especially on a dark background.
Important text does not stand out clearly from secondary or inactive text.

TASK:
- Adjust the UI so general readable text appears slightly whiter/brighter.
- Maintain a professional dark-theme look (not pure white everywhere).
- Keep disabled/placeholder text dimmer than regular content.
- Apply changes globally instead of per-component styling.

Implement this using Streamlit-compatible global styling or theming.
Avoid hacky per-widget fixes.

=====================================
PROBLEM 2 — MODEL CARD HTML EXPORT IS BROKEN
=====================================

The Model Card is generated using Markdown:
- In the UI preview, it looks fine.
- When downloaded as HTML and opened in a browser, it shows raw Markdown:
  - Headings like ## are visible
  - Tables show | characters
  - Code formatting is not rendered

At the same time, the Performance Report HTML is styled perfectly and looks professional.

This creates inconsistency and makes the Model Card feel unfinished.

ROOT CAUSE CONTEXT:
- Model Card HTML is currently produced by manually manipulating Markdown text (regex-style or direct write).
- The Performance Report uses real HTML generation and CSS styling.

TASK:
- Fix the backend so Model Card HTML is generated properly and matches the quality of the Performance Report.
- Convert Markdown to real HTML using a correct rendering approach.
- Wrap the result in a styled HTML template.
- Ensure tables, headings, lists, and code blocks render correctly.
- Do NOT regenerate the model card inside the UI.

ARCHITECTURAL RULES:
- Backend generates Markdown + HTML once after training
- UI only previews Markdown and downloads HTML
- Do not change existing Performance Report logic
- Keep changes minimal and clean

Also add some graphs in the performance report to make it look good
=====================================
WHAT I EXPECT FROM YOU
=====================================

1. Identify exactly where to update UI styling and apply global improvements.
2. Update Model Card HTML generation so downloaded Model Cards render correctly.
3. Provide the updated code where changes are needed.
4. Briefly explain what was changed and why (short explanation).

Please make the updates directly rather than just suggesting ideas.
