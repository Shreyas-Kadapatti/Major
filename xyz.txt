I am building an AutoML training pipeline where multiple scikit-learn models are trained, tuned, and evaluated.

I configured the pipeline to train 6 models, but only 4 models appear in the final leaderboard and evaluation results.

During training, the logs show that two models failed with parameter validation errors:
- ridge_classifier failed due to an invalid parameter passed to a CalibratedClassifierCV wrapper.
- passive_aggressive failed because a parameter (C) was passed to an SGDClassifier, where it is not valid.

The pipeline correctly:
- Logs these training failures
- Skips the failed models
- Continues training and evaluation with the remaining models
- Shows only successfully trained models in the leaderboard and downstream analysis

However, in the hyperparameter tuning summary, all 6 models appear, with the failed ones showing a score of 0.0.

Please explain:
1. Why only 4 models appear in the final results even though 6 were provided
2. Why failed models still appear in the tuning summary
3. Whether this behavior is correct for a robust AutoML system
4. What changes should be made to ensure all 6 models can be trained successfully

Assume the goal is production‑grade robustness and not forcing failed models to continue.
