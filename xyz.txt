You are a senior frontend engineer and ML platform architect.

I have an existing ML system UI implemented in Streamlit. The backend logic,
ML pipelines, drift detection, and explainability are already implemented in Python
and MUST NOT be re-written.

Your task is to convert the Streamlit-based dashboard into a modern React
frontend while keeping the backend unchanged.

==============================
PROJECT CONTEXT
==============================

Backend:
- Python-based AutoML pipeline
- Hypothesis-driven feature selection and feature engineering
- Model training, evaluation, SHAP explainability
- Drift detection using PSI, KS-test, and Chi-square
- SR 11-7 aligned (no auto-retraining, alert-only monitoring)
- Backend will be exposed via REST APIs (FastAPI / Flask)

Frontend (CURRENT):
- Implemented in Streamlit
- Pages include:
  1. Data Upload & Profiling
  2. Training & Model Selection
  3. Model Performance Dashboard
  4. Explainability (SHAP, PDP/ICE)
  5. Monitoring & Drift Detection

Frontend (TARGET):
- React (prefer Vite + React)
- UI-only responsibility
- Calls backend APIs using HTTP (fetch / axios)
- No ML logic in frontend

==============================
WHAT YOU SHOULD PRODUCE
==============================

1. ✅ A proposed React project structure, including:
   - pages/
   - components/
   - services/
   - hooks/ (if needed)

2. ✅ A clear mapping from Streamlit pages to React pages, for example:
   - Streamlit "Training" page → React TrainingPage.jsx
   - Streamlit "Monitoring & Drift" → React DriftMonitoringPage.jsx

3. ✅ API contract examples (request/response JSON)
   - /train
   - /predict
   - /monitor/drift
   - /results

4. ✅ Example React components (JSX only, no backend code):
   - File upload component
   - Button-triggered API call
   - Metrics cards (accuracy, F1, precision, recall)
   - Drift status banners (OK / WARNING / CRITICAL)
   - Tables for model leaderboard and drifted features

5. ✅ Recommended React libraries:
   - UI framework (MUI / Ant Design / Chakra)
   - Charts (Recharts / Plotly / ECharts)
   - API calls (axios)

6. ✅ Best practices to follow:
   - Environment variables for backend URLs
   - Loading + error states
   - No silent failures
   - No auto-retraining triggered from UI
   - Human-in-the-loop alerts only (SR 11-7)

==============================
IMPORTANT CONSTRAINTS
==============================

- DO NOT rewrite ML logic
- DO NOT reimplement drift algorithms in frontend
- DO NOT depend on Streamlit
- DO NOT add authentication unless explicitly requested
- Focus on clear UI, composable components, and API-driven design

==============================
OUTPUT FORMAT
==============================

- Explain the design decisions
- Provide clean JSX snippets
- Use modern React (hooks, functional components)
- Keep explanations concise and practical

Assume the reader understands Python ML but is new to React.
``
