You are a senior Python + FastAPI + ML engineer.

I have an existing FastAPI backend that currently supports ONLY inference:
- Loads model + preprocessor on startup
- Endpoints: /predict, /predict/batch, /health, /model/info, /reload
- Uses sklearn, a saved preprocessor, and a trained model
- Has PredictionRequest with `features` and `explain` fields

I want to EXTEND this SAME backend (not rewrite it) to support the FULL AutoML lifecycle used by my frontend.

I need you to:

1. Keep all existing inference functionality unchanged and working.
2. Add pipeline lifecycle management using in‑memory state (PipelineState pattern).
3. Add the following API endpoints to the same FastAPI app:

PIPELINE:
- POST /upload-data        (CSV/XLSX upload)
- POST /run-pipeline       (run AutoML training pipeline)
- GET  /pipeline-status    (progress, running, error)
- GET  /health             (show both inference and pipeline readiness)

RESULTS:
- GET /hypothesis          (hypothesis testing results)
- GET /models              (leaderboard, best model, trained models)
- GET /comparison          (McNemar or model comparison)
- POST /drift              (upload production data → drift detection)

PREDICTION:
- POST /predict            (already exists — keep)
- POST /batch-predict      (CSV batch prediction)

4. Introduce a clean PipelineState object that tracks:
- data_loaded
- is_trained
- running
- progress
- pipeline_result
- error

5. The training pipeline is already implemented as:
- TrainingPipeline.run(file_path, target_col, model_names, tune, callback)

Wire this pipeline into /run-pipeline.

6. After training completes:
- Save results into PipelineState
- Load the trained model artifacts into the existing inference service
- Ensure prediction endpoints immediately use the newly trained model

7. Implement endpoints in a production‑safe way:
- Proper error handling
- No global race conditions
- Minimal refactoring
- Clear separation between training (pipeline) and inference (prediction)

8. Use FastAPI background tasks where appropriate for long‑running training.

9. Provide COMPLETE, runnable code for:
- Updated `app.py`
- PipelineState class
- Any added helper functions

IMPORTANT:
- Do NOT remove existing endpoints
- Do NOT break `/predict`
- Do NOT change request schemas unless necessary
- Use clean, readable code with comments

The final output should be a fully working FastAPI backend that supports BOTH:
- AutoML pipeline orchestration
- Production inference
ADD PREDICTION SCHEMA IN THE BACKEND

Explain briefly how the added endpoints integrate with the existing inference layer.
